package {{GROUP_ID}}.config;

import org.springframework.context.annotation.Configuration;

/**
 * Application configuration for {{PROJECT_NAME}}.
 */
@Configuration
public class AppConfig {
    
    // Add custom configuration beans here
}
