# {{OB_PROJECT_NAME}}

{{DESCRIPTION}}

## Overview

| Property | Value |
|----------|-------|
| **Owner** | {{OWNER}} |
| **Language** | Java {{JAVA_VERSION}} |
| **Framework** | Spring Boot {{SPRING_BOOT_VERSION}} |
| **Build Tool** | Maven |
| **Port** | {{PORT}} |

## Quick Start

### Prerequisites

- Java {{JAVA_VERSION}} or higher (JDK)
- Maven 3.8+ (or use included wrapper)
- Docker (for containerized deployment)

### Clone and Build

```bash
git clone <repository-url>
cd {{OB_PROJECT_NAME}}
./mvnw clean package
```

### Run Locally

```bash
# Using Maven wrapper
./mvnw spring-boot:run

# Or run the JAR directly
java -jar target/{{OB_PROJECT_NAME}}-0.0.1-SNAPSHOT.jar
```

The service will be available at `http://localhost:{{PORT}}`

### Run Tests

```bash
./mvnw test
```

## API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/actuator/health` | Health check endpoint |
| GET | `/actuator/info` | Application info |
| GET | `/api/v1/hello` | Sample greeting endpoint |

## Project Structure

```
{{OB_PROJECT_NAME}}/
├── src/
│   ├── main/
│   │   ├── java/{{PACKAGE_PATH}}/
│   │   │   ├── Application.java
│   │   │   ├── config/
│   │   │   │   └── AppConfig.java
│   │   │   ├── controller/
│   │   │   │   └── HelloController.java
│   │   │   └── service/
│   │   │       └── HelloService.java
│   │   └── resources/
│   │       ├── application.yml
│   │       └── application-dev.yml
│   └── test/
│       └── java/{{PACKAGE_PATH}}/
│           └── ApplicationTests.java
├── Dockerfile
├── pom.xml
├── mvnw
├── mvnw.cmd
└── README.md
```

## Docker

### Build Image

```bash
docker build -t {{OB_PROJECT_NAME}}:latest .
```

### Run Container

```bash
docker run -p {{PORT}}:{{PORT}} {{OB_PROJECT_NAME}}:latest
```

## Environment Variables

| Variable | Description | Default |
|----------|-------------|---------|
| `SERVER_PORT` | Server port | {{PORT}} |
| `SPRING_PROFILES_ACTIVE` | Active Spring profile | default |
| `JAVA_OPTS` | JVM options | -Xms256m -Xmx512m |

## Spring Actuator Endpoints

| Endpoint | Description |
|----------|-------------|
| `/actuator/health` | Application health |
| `/actuator/health/liveness` | Kubernetes liveness probe |
| `/actuator/health/readiness` | Kubernetes readiness probe |
| `/actuator/info` | Application information |
| `/actuator/metrics` | Application metrics |

## Deployment

This project is automatically deployed via Azure DevOps pipeline on push to `main` branch.

### Target Environments

- `dev` - Development environment

## Support

For issues or questions, contact: **{{OWNER}}**

---

*Generated by k8s-ops-box Self-Service Portal*
